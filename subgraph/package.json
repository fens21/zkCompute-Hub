{
  "name": "job-marketplace-subgraph",
  "version": "0.1.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ job-marketplace",
    "deploy:goldsky": "goldsky subgraph deploy job-marketplace/1.0.0 --path ."
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.95.0",
    "@graphprotocol/graph-ts": "^0.35.0"
  }
}
